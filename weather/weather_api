from flask import Flask
from flask import jsonify
from main import weather_outside
app = Flask(__name__)
weather = weather_outside()

#print(weather)
@app.route('/')
def homepage():
    return jsonify(weather)

if __name__ == '__main__':
    app.run(use_reloader=True, debug=True)
